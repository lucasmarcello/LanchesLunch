@model CarrinhoCompraViewModel

@if (Model.CarrinhoCompra.ListCarrinhoCompraItens.Count > 0)
{
    <li>
        <a asp-controller="CarrinhoCompra">
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.CarrinhoCompra.ListCarrinhoCompraItens.Count
            </span>
        </a>
    </li>
}
